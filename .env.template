# EcomIntent API — Environment Configuration
# DO NOT COMMIT THIS FILE. Confirm it's in .gitignore.
# Copy to .env and fill in all values before running any scripts.

# Anthropic API (for synthetic data generation)
ANTHROPIC_API_KEY=

# HuggingFace (for model publishing)
HF_TOKEN=
HF_USERNAME=
HF_MODEL_REPO=

# Modal.com (for deployment)
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=
#modal token set --token-id <your_token_id> --token-secret <your_token_secret>

# Model config (auto-set by training, verify after)
MODEL_NAME=distilbert-base-uncased
